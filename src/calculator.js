export const CONSTANTS={
SIMULATION_COUNT:500000,
EXPECTED_RANGE_LOW_PERCENTILE:50,
EXPECTED_RANGE_HIGH_PERCENTILE:90,
MAX_COLLECTION_SIZE:256,
MAX_ELEMENTS_PER_CONTAINER:100,
MAX_DUPLICATE_RATE:1000
};

// Pure functions only:
// simulateOneRun()
// runSimulation()
// calculateBestCase()
// calculateWorstCase()
// percentile()
